return {
  'brenoprata10/nvim-highlight-colors',
  opts = {
    render = 'foreground',
    virtual_symbol = require('jfan.icons').ui.Round,
    enable_named_colors = false,
    enable_tailwind = true,
    custom_colors = {},
  },
}
